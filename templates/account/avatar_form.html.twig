{% extends 'base.html.twig' %}

{% block title %}Gérer mon profil  - Ma boutique{% endblock %}

{% block stylesheet %}
    {# <link href={{ asset('assets/css/profileForm.css')}} rel="stylesheet"> #}
{% endblock %}

{% block body %}

<div class="container">

{# <a href="" class="btn btn-warning text-dark btn-md float-right">Modifier mon informations de profil</a> #}

<h1 class="text-warning">Gérer mon profil<h1>
{# <a href="{{ path('account_profile') }}">Retour</a> #}
<div class="dropdown-divider"></div>

{% if notification %}
	<div class="container col-md-8 mt-5">
		<div class="bg-success rounded-lg text-light text-center p-2"><i class="far fa-check-circle"></i> {{ notification }}</div>
	</div>
{% endif %}


{# <div class="bg-light p-4"> #}
    {# {{ form(form) }}
     #}
{# </div> #}

<div class="container col-md-6 bg-light rounded-lg my-5 p-4">

{{ form_start(form) }}

<div class="row">
<div class="col-md-9">
        {{ form_row(form.avatar) }}
    </div>
<div class="col-md-3 mb-5">
    {% if app.user.avatar == null or app.user.avatar == "e29d5ce22434c6895e856fd36360d66b.png" %}
    <img src="{{ asset('assets/img/profile-user.png') }}" alt="Avatar de {{ app.user.firstname }}" class="img-fluid" style="font-size: 2.5rem;" width="100" height="120"/>
    {% else %}
    <img src="/uploads/avatar/{{ app.user.avatar }}" alt="Avatar de {{ app.user.firstname }}" class="img-fluid border border-dark text-center" style="font-size: 2.5rem;" width="100" height="120"/>
    <a href="{{ path('account_avatar_delete', {'id' : app.user.id}) }}" style="font-size: 1.3rem; color: black;"><u>Supprimer</u></a>
    {% endif %}
</div>
</div>
<div class="dropdown-divider mb-4"></div>
<div class="col-md-4 m-auto">
    {{ form_row(form.submit) }}
</div>
    {# <h2>Avatar actuel :</h2>
    {% for avatar in app.user.avatar %}
    <div>
        <img src="{{ asset('/uploads/avatar/'~ app.user.avatar) }}" alt="Avatar" width="100px">
    </div>
    {% endfor %} #}

    {# <div class="col-md-12 mb-3">{{ form_row(form.birthday) }}</div> #}


{{ form_end(form) }}

</div>


{# {{ form_start(form) }}
<div class="row bg-light p-4">
    <div class="col-md-12">{{ form_row(form.email) }}</div>
    <div class="col-md-6">{{ form_row(form.avatar) }}</div>
    <div class="col-md-6">{{ form_row(form.birthday) }}</div> #}
    {# <div class="col-md-6">{{ form_row(form.company) }}</div>
    <div class="col-md-6">{{ form_row(form.address) }}</div>
    <div class="col-md-6">{{ form_row(form.postal) }}</div>
    <div class="col-md-6">{{ form_row(form.city) }}</div>
    <div class="col-md-6">{{ form_row(form.country) }}</div>
    <div class="col-md-6">{{ form_row(form.phone) }}</div> #}
{# </div>
{{ form_end(form) }} #}


{# {{ form_start(form) }}

    {{ form_widget(form) }}
{{ form_end(form) }} #}



<div class="dropdown-divider"></div>

{# <a href="{{ path('account_profile') }}"><b class="text-left text-dark"><img src="{{ asset('assets/img/back-arrow.png') }}" alt="Mes commandes"> Retour</b></a> #}
        <div class="container mb-5">
        {# <div class="dropdown-divider mt-4"></div> #}
        <a href="{{ path('account_profile') }}" class="text-light text-left" style="font-size: 1.2rem; text-decoration: none;"><i class="fas fa-arrow-circle-left"></i> RETOUR</b></a>
        </div>

</div>
</div>

{% endblock %}

