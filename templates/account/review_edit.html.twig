{% extends 'base.html.twig' %}

{% block title %}Gérer mes commentaires - Rhuma Sug{% endblock %}

{% block body %}

<div class="container">

{# <a href="" class="btn btn-warning text-dark btn-md float-right">Modifier un commentaire</a> #}

{# <h1><img src="{{ asset('assets/img/letterbox-1.png') }}" alt="Photo de profil" style="width: 60px; height: 60px; border-radius:100%"> Gérer mes adresses<h1> #}
<h1 class="text-warning">MODIFIER MON COMMENTAIRE<h1>
{# <a href="{{ path('account_address') }}">Retour</a> #}
<div class="dropdown-divider"></div>

{# {{ form(form) }} #}

<div class="container bg-light rounded-lg my-5 p-4">

{{ form_start(form) }}
<div class="row">
    <div class="col-md-8">
        <p>{{ form_row(form.comment) }}</p>
    </div>
    <div class="col-md-4 mt-3">
        <div>{{ form_row(form.note) }}</div>
    </div>
</div>
<hr class="mt-4 mb-5">
<div class="row">
    <div class="col-md-3 m-auto">{{ form_row(form.submit) }}</div>
</div>
{{ form_end(form) }}

</div>
{# 
<hr class="mt-4"> #}

<div class="dropdown-divider"></div>
        <div class="container mb-5">
        <a href="{{ path('account_order') }}" class="text-light text-left" style="font-size: 1.2rem; text-decoration: none;"><i class="fas fa-arrow-circle-left"></i> RETOUR</b></a>
        </div>

</div>

    

{% endblock %}