{% extends 'base.html.twig' %}

{% block title %}Gérer mes adresses  - Ma boutique{% endblock %}

{% block body %}

<div class="container">

<a href="" class="btn btn-warning text-dark btn-md float-right"><i class="fas fa-plus"></i> AJOUTER UNE ADRESSE</a>

{# <h1><img src="{{ asset('assets/img/letterbox-1.png') }}" alt="Photo de profil" style="width: 60px; height: 60px; border-radius:100%"> Gérer mes adresses<h1> #}
<h1 class="text-warning">GERER MES ADRESSES<h1>
{# <a href="{{ path('account_address') }}">Retour</a> #}
<div class="dropdown-divider"></div>

{# {{ form(form) }} #}

{# {% for address in address %} #}
{% if addresses | length == 0 %}
    <div class="container bg-danger rounded-lg mt-4 p-3">
        <h1 class="text-light text-center"><i class="fas fa-exclamation-triangle"></i> Vous devez obligatoirement renseigner une adresse de livraison pour pouvoir finaliser vos achats.</h1>
    </div>
{% endif %}
{# {% endfor %} #}

<div class="container alert alert-warning rounded-lg my-5 p-4">

{{ form_start(form) }}
<div class="row">
    <div class="col-md-3 text-center">
        <img src="https://i.ibb.co/gMY7vbf/map.png" alt="map_picture" class="border border-dark" width="150px", height="120px">
    </div>
    <div class="col-md-6 text-left">
        <div>{{ form_row(form.name) }}</div>
    </div>
</div>
{# <div class="col-md-6 m-auto mb-5">
    <div>{{ form_row(form.name) }}</div>
</div> #}
<hr class="mt-4" style="border: 1px solid black;">
{# <div class="text-center mt-4 mb-4">
        <span class="dot mr-4 text-warning"></span>
        <span class="dot mr-4 text-dark"></span>
        <span class="dot mr-4 text-dark"></span>
        <span class="dot mr-4 text-dark"></span>
    </div> #}
<div class="row mt-2">
    <div class="col-md-4">{{ form_row(form.firstname) }}</div>
    <div class="col-md-4">{{ form_row(form.lastname) }}</div>
    <div class="col-md-4">{{ form_row(form.company) }}</div>
</div>
<div class="row mt-2">
    <div class="col-md-8">{{ form_row(form.address) }}</div>
    <div class="col-md-4">{{ form_row(form.zipCode) }}</div>
</div>
<div class="row mt-2">
    <div class="col-md-4">{{ form_row(form.city) }}</div>
    <div class="col-md-4">{{ form_row(form.country) }}</div>
    <div class="col-md-4">{{ form_row(form.phone) }}</div>
</div>
<hr class="mt-4 mb-5" style="border: 1px solid black;">
<div class="row">
    <div class="col-md-3 m-auto">{{ form_row(form.submit) }}</div>
</div>
{{ form_end(form) }}

</div>
{# 
<hr class="mt-4"> #}

<div class="dropdown-divider"></div>
        <div class="container mb-5">
        <a href="{{ path('account_address') }}" class="text-light text-left" style="font-size: 1.2rem; text-decoration: none;"><i class="fas fa-arrow-circle-left"></i> RETOUR</b></a>
        </div>

</div>

    

{% endblock %}